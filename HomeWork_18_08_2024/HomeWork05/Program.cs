namespace HomeWork05;

class Program
{
    static void Main(string[] args)
    {
        // bool tipinde iki değişken oluşturuldu ve değerleri atandı
        bool durum1 = true;
        bool durum2 = false;

        // Bu değişkenleri ekrana yazdırdım
        Console.WriteLine("Durum1: " + durum1);
        Console.WriteLine("Durum2: " + durum2);
    }
}
